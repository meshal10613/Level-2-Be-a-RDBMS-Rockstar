/**
 ** Some PostgreSQL Commands- in psql shell
 * 
 * ? list of database-> \l
 * ? list of tables-> \dt
 * ? list of columns-> \d
 * ? list of connection information-> \conninfo
 * ? list of roles-> \du
 * ? list of all commands-> \?
 * ? for quit-> \q
 * ? check version-> select version();
 */